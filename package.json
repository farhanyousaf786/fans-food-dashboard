{
  "name": "fansfood-app",
  "version": "1.0.0",
  "description": "Stadium food ordering application",
  "main": "index.js",
  "scripts": {
    "start": "cd server && npm start",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install && npm run install-all && cd client && npm run build"
  },
  "keywords": ["stadium", "food", "ordering", "react", "express"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
