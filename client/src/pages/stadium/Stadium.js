import React from 'react';
import { Box, Typography } from '@mui/material';

const Stadium = () => {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4">Stadium</Typography>
            <Typography variant="body1" color="text.secondary">Stadium management content will be added here.</Typography>
        </Box>
    );
};

export default Stadium;
