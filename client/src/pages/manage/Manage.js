import React from 'react';
import { Box, Typography } from '@mui/material';

const Manage = () => {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4">Manage</Typography>
            <Typography variant="body1" color="text.secondary">Management page content will be added here.</Typography>
        </Box>
    );
};

export default Manage;
